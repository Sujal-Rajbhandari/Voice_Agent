# Python 3.10+ required (3.12 recommended). Install with:
#   python3.12 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
livekit-agents[deepgram,openai,silero]>=1.6,<2
python-dotenv>=1.0
streamlit>=1.35
pandas>=2.0
